conda { 
  cacheDir = "${params.condaCacheDir}" 
}

/*
 * Add here the list of label and the same main conda
 */

process {
  withLabel:python { conda = "${baseDir}/environment.yml" }
  withLabel:multiqc { conda = "${baseDir}/environment.yml" }
}
